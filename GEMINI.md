# DSL Modification Protocol

If a grammar in `dsl/` changes (validation, structure, etc.), you MUST:
1. **Sync Docs**: Update technical reference manuals in `dsl/`.
2. **Sync Validation**: Update Langium validator, JSON schema, and all examples.
3. **Sync Frontend**: Update UI schemas, limits, field names, and JSON editor validation.